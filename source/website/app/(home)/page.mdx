{/* Vendor Modules */}

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

{/* Website Modules */}

import { Hero, Footer, Install } from '@/website/components';

<Hero className="mb-10" />

<h2 className="text-2xl">
    Talos aims to provide a fast, flexible, and robust language/toolkit for modern programming development.
</h2>

<Install className="text-2xl italic mt-10 mb-4" />

<h2 className="text-2xl mt-10 mb-4">
    Powered with builtin type-safety, a feature-rich standard library, and a highly fault-tolerant concurrent runtime.
</h2>

```talos tab="hello.tal"
Debug.println("Hello, 🦄!");
```

```talos tab="types.tal"
let add = fn (a: Number, b: Number): Number => a + b;
Debug.println("5 + 7 = {0}".fmt(add(5, 7))); // typed
```

```talos tab="future.tal"
for (ch, n in "Talos") {
    Debug.println("{0}: {1}".fmt(n, ch));
    Future.delay(1000).await(); // wait
}
```

```talos tab="testing.tal"
import "talos:test" as Test;
import "talos:assert" as Assert;

Test.suite("Addition", fn {
    Test.case("5 + 7 = 12", fn => Assert.equals(5 + 7, 12));
    Test.bench("Benchmark", fn => 5 + 7); // test performance
});
```

<h2 className="text-2xl mt-10 mb-4">
    Talos comes with batteries-included, so all the essential tools you need to build, test and deploy applications are
    included out of the box.
</h2>

<Tabs items={['Execute', 'Lint', 'Format', 'Test', 'Deploy']}>
    <Tab value="Execute">
        Whether it be simple scripts, tasks or entire projects, Talos can [execute](/language/toolkit/runtime) it all.
    </Tab>
    <Tab value="Lint">
        A builtin [linter](/language/toolkit/linting) to help maintain code consistency and avoid bugs.
    </Tab>
    <Tab value="Format">[Formatting](/language/toolkit/formatter) support that prettifies Talos files.</Tab>
    <Tab value="Test">
        Talos provides a [test-runner](/language/toolkit/testing) and assertion libraries to ensure code accuracy during
        development.
    </Tab>
    <Tab value="Deploy">
        Seamlessly create [standalone native executables](/language/toolkit/bundler) from your Talos program.
    </Tab>
</Tabs>

<Footer className="mt-10" />
