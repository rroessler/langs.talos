## Executable Injection - `xjct`

This library wraps the [LIEF project](https://github.com/lief-project/LIEF) to allow for bundling executables with user metadata. This is similar to how the [`node:sea`](https://nodejs.org/api/single-executable-applications.html) package operates, but we enable more flexibility by also exposing customizable binary fuses.
