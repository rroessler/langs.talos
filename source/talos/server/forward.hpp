#ifndef _TALOS_SERVER_FORWARD_HPP
#define _TALOS_SERVER_FORWARD_HPP

/// Forge Modules
#include <forge/server/connection.hpp>

/// Forward Declarations
$_FWD(using Options = Forge::Server::Options, Talos::Server)
$_FWD(using Connection = Forge::Server::Connection, Talos::Server)

#endif
