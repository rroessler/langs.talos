## Testing Framework - `xtest`

This is the custom testing framework for Talos. Currently it is being used by both Talos (via the `talos:test` crate) and internally (as the test-runner for the monorepo itself).
